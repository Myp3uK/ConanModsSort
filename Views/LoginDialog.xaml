<Window x:Class="ConanModsSort.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        SizeToContent="Height" Width="420"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="False"
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner" Background="#1C1C25"
        FontFamily="Segoe UI" TextOptions.TextFormattingMode="Display">
    <Window.Resources>
        <Style x:Key="DlgBtn" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="94"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2A2A36"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="6" Background="{TemplateBinding Background}"
                                Padding="14,0" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#363645"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DlgAccent" TargetType="Button" BasedOn="{StaticResource DlgBtn}">
            <Setter Property="Background" Value="#3A7BD5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="6" Background="{TemplateBinding Background}"
                                Padding="14,0" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#4C8DEA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="#12121A"/>
            <Setter Property="Foreground" Value="#E8E8EE"/>
            <Setter Property="CaretBrush" Value="#E8E8EE"/>
            <Setter Property="BorderBrush" Value="#33333F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="#12121A"/>
            <Setter Property="Foreground" Value="#E8E8EE"/>
            <Setter Property="CaretBrush" Value="#E8E8EE"/>
            <Setter Property="BorderBrush" Value="#33333F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#3A7BD5" BorderThickness="1" Padding="20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,14" Background="Transparent"
                        MouseLeftButtonDown="TitleBar_Drag">
                <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="20" Text="&#xE77B;"
                           Foreground="#3A7BD5" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Вход в Steam" Foreground="#E8E8EE" FontSize="15" FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock Text="Логин и пароль используются один раз для получения токена. Пароль нигде не сохраняется — хранится только зашифрованный токен для входа без пароля."
                       Foreground="#9A9AA6" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,14"/>

            <TextBlock Text="Логин Steam" Foreground="#C8C8D2" FontSize="12" Margin="0,0,0,4"/>
            <TextBox x:Name="txtUser" Margin="0,0,0,12"/>

            <TextBlock Text="Пароль" Foreground="#C8C8D2" FontSize="12" Margin="0,0,0,4"/>
            <PasswordBox x:Name="txtPass" Margin="0,0,0,18"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Войти" Style="{StaticResource DlgAccent}" IsDefault="True" Click="Ok_Click"/>
                <Button Content="Отмена" Style="{StaticResource DlgBtn}" IsCancel="True" Click="Cancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
