<Window x:Class="ConanModsSort.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Conan Exiles — менеджер модов" Icon="/Icon.ico"
        Height="960" Width="1280" MinHeight="460" MinWidth="700"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize"
        StateChanged="Window_StateChanged"
        Background="#13131A" FontFamily="Segoe UI" TextOptions.TextFormattingMode="Display">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="48" ResizeBorderThickness="6"
                            CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="Bg"          Color="#13131A"/>
        <SolidColorBrush x:Key="Panel"       Color="#1C1C25"/>
        <SolidColorBrush x:Key="ListBg"      Color="#181820"/>
        <SolidColorBrush x:Key="Border"      Color="#2E2E3A"/>
        <SolidColorBrush x:Key="ItemHover"   Color="#26263200"/>
        <SolidColorBrush x:Key="Text"        Color="#E8E8EE"/>
        <SolidColorBrush x:Key="SubText"     Color="#8A8A98"/>
        <SolidColorBrush x:Key="Accent"      Color="#3A7BD5"/>
        <SolidColorBrush x:Key="AccentHover" Color="#4C8DEA"/>
        <SolidColorBrush x:Key="Secondary"   Color="#2A2A36"/>
        <SolidColorBrush x:Key="SecondaryHv" Color="#363645"/>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="ScrollThumb" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="t" CornerRadius="5" Background="#3C3C49" Margin="2,0"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="t" Property="Background" Value="#52526A"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="t" Property="Background" Value="#5A9BF5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollNoBtn" TargetType="RepeatButton">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="11"/>
            <Setter Property="MinWidth" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Border Background="Transparent" CornerRadius="5">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollNoBtn}"
                                                  Command="ScrollBar.PageUpCommand"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollThumb}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollNoBtn}"
                                                  Command="ScrollBar.PageDownCommand"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Width" Value="Auto"/>
                                <Setter Property="Height" Value="11"/>
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CaptionButton" TargetType="Button">
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Foreground" Value="#C8C8D2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="6" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#2A2A36"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource CaptionButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="6" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#C42B1C"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FlatButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource Secondary}"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="6" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource SecondaryHv}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="b" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="b" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource FlatButton}">
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="6" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource AccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="b" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PlayButton" TargetType="Button" BasedOn="{StaticResource FlatButton}">
            <Setter Property="Background" Value="#2E7D32"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="6" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#388E3C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="b" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource FlatButton}">
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="15"/>
        </Style>

        <Style x:Key="PathBox" TargetType="TextBox">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="{StaticResource Panel}"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="6" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="ModTemplate">
            <Grid Margin="2">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding NotDownloaded}" Value="True">
                                <Setter Property="Opacity" Value="0.6"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Открыть страницу в Steam" Click="OpenModPage_Click">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xE774;" FontFamily="Segoe MDL2 Assets" FontSize="13"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Открыть папку мода" Click="OpenModFolder_Click">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xE838;" FontFamily="Segoe MDL2 Assets" FontSize="13"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Копировать ID мода" Click="CopyModId_Click">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" FontSize="13"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator Background="{StaticResource Border}" Height="1" Margin="6,4"/>
                        <MenuItem Header="Удалить мод (папку с диска)" Click="DeleteMod_Click"
                                  Foreground="#E0715E">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="13"
                                           Foreground="#E0715E"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </Grid.ContextMenu>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="46" Height="46" CornerRadius="5"
                        Background="#2A2A36" ClipToBounds="True">
                    <Grid>
                        <TextBlock Text="—" Foreground="#555562" FontSize="18"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Image Source="{Binding Thumb}" Stretch="UniformToFill"/>
                    </Grid>
                </Border>
                <StackPanel Grid.Column="1" Margin="10,0,4,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Title}" Foreground="{StaticResource Text}"
                               FontSize="13.5" FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"/>
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                        <TextBlock Text="{Binding SubText}" Foreground="{StaticResource SubText}"
                                   FontSize="11" VerticalAlignment="Center"/>
                        <Border Background="#E0A030" CornerRadius="4" Padding="5,1" Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                Visibility="{Binding NeedsUpdate, Converter={StaticResource BoolToVis}}"
                                ToolTip="Доступно обновление в Steam Workshop">
                            <TextBlock Text="обновление" Foreground="#1A1A1A"
                                       FontSize="10" FontWeight="SemiBold"/>
                        </Border>
                        <Border Background="#C25A4A" CornerRadius="4" Padding="5,1" Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                Visibility="{Binding NotDownloaded, Converter={StaticResource BoolToVis}}"
                                ToolTip="Мод ещё не скачан — нажмите «Скачать» или обновите список">
                            <TextBlock Text="не скачан" Foreground="White"
                                       FontSize="10" FontWeight="SemiBold"/>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <Style x:Key="ModItemStyle" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="b" CornerRadius="7" Background="Transparent"
                                BorderBrush="Transparent" BorderThickness="1"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#23232E"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#243B63"/>
                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModList" TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource ListBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="ItemTemplate" Value="{StaticResource ModTemplate}"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ModItemStyle}"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border CornerRadius="8" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Margin="2" Padding="{TemplateBinding Padding}">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ColumnHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="4,0,0,6"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource SubText}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12.5"/>
        </Style>

        <Style x:Key="TabRadio" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource SubText}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="b" CornerRadius="7" Background="Transparent"
                                Padding="16,7" Margin="0,0,6,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#23232E"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource Accent}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkComboItem" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="b" Background="Transparent" CornerRadius="5"
                                Margin="3,2" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#23232E"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#243B63"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkCombo" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="Background" Value="{StaticResource Panel}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="IsEditable" Value="True"/>
            <Setter Property="StaysOpenOnEdit" Value="True"/>
            <Setter Property="IsTextSearchEnabled" Value="True"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="26"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="bg" Grid.ColumnSpan="2" CornerRadius="6"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"/>
                            <TextBox x:Name="PART_EditableTextBox" Grid.Column="0"
                                     Margin="9,0,0,0" VerticalAlignment="Center"
                                     Background="Transparent" BorderThickness="0"
                                     Foreground="{StaticResource Text}" CaretBrush="{StaticResource Text}"/>
                            <ToggleButton Grid.Column="1" Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent">
                                            <Path Data="M0,0 L4,4 L8,0" Stroke="{StaticResource SubText}"
                                                  StrokeThickness="1.5" StrokeStartLineCap="Round"
                                                  StrokeEndLineCap="Round"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="Popup" Placement="Bottom" Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" PopupAnimation="Slide"
                                   MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                <Border Background="{StaticResource Panel}" CornerRadius="6"
                                        BorderBrush="{StaticResource Border}" BorderThickness="1"
                                        Margin="0,4,0,0" MaxHeight="280">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="14"
                                                          ShadowDepth="0" Opacity="0.5"/>
                                    </Border.Effect>
                                    <ScrollViewer><ItemsPresenter/></ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="bg" Property="BorderBrush" Value="{StaticResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource Panel}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Padding="4">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="16" ShadowDepth="0" Opacity="0.5"/>
                            </Border.Effect>
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="Padding" Value="9,7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="b" Background="Transparent" CornerRadius="5"
                                Margin="2" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="22"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" ContentSource="Icon"
                                                  VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <ContentPresenter Grid.Column="1" ContentSource="Header"
                                                  VerticalAlignment="Center" Margin="8,0,4,0"
                                                  RecognizesAccessKey="True"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#23232E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="WindowBorder" Background="{StaticResource Bg}"
            BorderBrush="{StaticResource Border}" BorderThickness="1">
    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="{StaticResource Bg}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                <Border Width="28" Height="28" CornerRadius="7" Margin="0,0,10,0">
                    <Border.Background>
                        <ImageBrush ImageSource="/Icon.png" Stretch="UniformToFill"/>
                    </Border.Background>
                </Border>
                <TextBlock Text="Conan Exiles — менеджер модов" Foreground="{StaticResource Text}"
                           FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                        VerticalAlignment="Top" Margin="0,6,8,0">
                <Button x:Name="btnMin" Style="{StaticResource CaptionButton}" Content="&#xE921;"
                        ToolTip="Свернуть" Click="Minimize_Click"/>
                <Button x:Name="btnMax" Style="{StaticResource CaptionButton}" Content="&#xE922;"
                        ToolTip="Развернуть" Click="Maximize_Click"/>
                <Button x:Name="btnClose" Style="{StaticResource CloseButton}" Content="&#xE8BB;"
                        ToolTip="Закрыть" Click="Close_Click"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="16,4,16,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Папка модов" Style="{StaticResource FieldLabel}"/>
            <TextBox Grid.Column="1" x:Name="txtModsFolder" Style="{StaticResource PathBox}"/>
            <Button Grid.Column="2" Content="Выбрать…" Margin="8,0,0,0"
                    Style="{StaticResource FlatButton}" Click="ChooseModsFolder_Click"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="modlist.txt" Style="{StaticResource FieldLabel}"/>
            <TextBox Grid.Column="1" x:Name="txtModlist" Style="{StaticResource PathBox}"/>
            <Button Grid.Column="2" Content="Выбрать…" Margin="8,0,0,0"
                    Style="{StaticResource FlatButton}" Click="ChooseModlist_Click"/>
        </Grid>

        <DockPanel Grid.Row="2">
        <Grid DockPanel.Dock="Top" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Border Background="{StaticResource Panel}" CornerRadius="8" Padding="4">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="tabEnhanced" Style="{StaticResource TabRadio}" GroupName="ver"
                                     Content="Enhanced" IsChecked="True" Checked="Enhanced_Checked"/>
                        <RadioButton x:Name="tabLegacy" Style="{StaticResource TabRadio}" GroupName="ver"
                                     Content="Legacy" Checked="Legacy_Checked"/>
                    </StackPanel>
                </Border>
                <Button Content="&#xE117;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource IconButton}"
                        Margin="10,0,0,0" VerticalAlignment="Center" Click="UpdateMods_Click"
                        ToolTip="Проверить обновления модов через Steam и докачать нужные (SteamCMD)"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="Профиль" Style="{StaticResource FieldLabel}" Margin="0,0,8,0"/>
                <ComboBox x:Name="cmbPreset" Style="{StaticResource DarkCombo}" Width="190"
                          DisplayMemberPath="Name" TextSearch.TextPath="Name"
                          DropDownClosed="Preset_DropDownClosed"/>
                <TextBlock x:Name="txtPresetDirty" Text="● не сохранён" Foreground="#E0A030"
                           FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"
                           Margin="8,0,0,0" Visibility="Collapsed"
                           ToolTip="Список изменён — нажмите «Сохранить», чтобы обновить профиль"/>
                <Button Content="Сохранить" Margin="8,0,0,0"
                        Style="{StaticResource FlatButton}" Click="SavePreset_Click"
                        ToolTip="Сохранить текущий порядок текущей вкладки как профиль (имя — в поле слева)"/>
                <Button Content="Удалить" Margin="6,0,0,0"
                        Style="{StaticResource FlatButton}" Click="DeletePreset_Click"
                        ToolTip="Удалить выбранный профиль"/>
            </StackPanel>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock DockPanel.Dock="Top" Text="Доступные моды" Style="{StaticResource ColumnHeader}"/>
                <ListBox x:Name="lstAvailable" Style="{StaticResource ModList}"
                         AllowDrop="True"
                         PreviewMouseLeftButtonDown="List_PreviewMouseLeftButtonDown"
                         PreviewMouseMove="List_PreviewMouseMove"
                         DragOver="List_DragOver" DragLeave="List_DragLeave" Drop="List_Drop"
                         MouseDoubleClick="Available_DoubleClick"/>
            </DockPanel>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                <Button Content="▶" Style="{StaticResource IconButton}" Margin="0,0,0,8"
                        ToolTip="Добавить в порядок" Click="MoveRight_Click"/>
                <Button Content="◀" Style="{StaticResource IconButton}" Margin="0,0,0,8"
                        ToolTip="Убрать из порядка" Click="MoveLeft_Click"/>
                <Button Content="▲" Style="{StaticResource IconButton}" Margin="0,0,0,8"
                        ToolTip="Выше" Click="MoveUp_Click"/>
                <Button Content="▼" Style="{StaticResource IconButton}"
                        ToolTip="Ниже" Click="MoveDown_Click"/>
            </StackPanel>

            <DockPanel Grid.Column="2">
                <TextBlock DockPanel.Dock="Top" Text="Порядок загрузки (сверху вниз)"
                           Style="{StaticResource ColumnHeader}"/>
                <ListBox x:Name="lstOrdered" Style="{StaticResource ModList}"
                         AllowDrop="True"
                         PreviewMouseLeftButtonDown="List_PreviewMouseLeftButtonDown"
                         PreviewMouseMove="List_PreviewMouseMove"
                         DragOver="List_DragOver" DragLeave="List_DragLeave" Drop="List_Drop"
                         MouseDoubleClick="Ordered_DoubleClick"/>
            </DockPanel>

            <Border x:Name="busyOverlay" Grid.ColumnSpan="3" Panel.ZIndex="10"
                    Background="#CC0E0E14" CornerRadius="8" Visibility="Collapsed">
                <Border Background="{StaticResource Panel}" CornerRadius="12"
                        BorderBrush="{StaticResource Accent}" BorderThickness="1"
                        Padding="34,26" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="28" ShadowDepth="0" Opacity="0.7"/>
                    </Border.Effect>
                    <StackPanel HorizontalAlignment="Center">
                        <Canvas Width="48" Height="48" RenderTransformOrigin="0.5,0.5">
                            <Ellipse Width="48" Height="48" Stroke="#33FFFFFF" StrokeThickness="5"/>
                            <Path Stroke="{StaticResource Accent}" StrokeThickness="5"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round">
                                <Path.Data>
                                    <PathGeometry>
                                        <PathFigure StartPoint="24,2.5">
                                            <ArcSegment Point="2.5,24" Size="21.5,21.5"
                                                        IsLargeArc="True" SweepDirection="Clockwise"/>
                                        </PathFigure>
                                    </PathGeometry>
                                </Path.Data>
                            </Path>
                            <Canvas.RenderTransform>
                                <RotateTransform x:Name="spin"/>
                            </Canvas.RenderTransform>
                        </Canvas>
                        <TextBlock x:Name="busyText" Text="Загрузка…" Foreground="{StaticResource Text}"
                                   FontSize="14" FontWeight="SemiBold" Margin="0,16,0,0"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Border>
        </Grid>
        </DockPanel>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="▶ Запустить игру" Style="{StaticResource PlayButton}"
                        Margin="0,0,12,0" Click="LaunchGame_Click"
                        ToolTip="Запустить Conan Exiles"/>
                <TextBlock x:Name="txtStatus" Foreground="{StaticResource SubText}"
                           VerticalAlignment="Center" FontSize="12.5" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Импорт" Margin="0,0,8,0"
                        Style="{StaticResource FlatButton}" Click="Import_Click"
                        ToolTip="Загрузить список модов из буфера обмена"/>
                <Button Content="Поделиться" Margin="0,0,8,0"
                        Style="{StaticResource FlatButton}" Click="Share_Click"
                        ToolTip="Скопировать список модов текущей вкладки в буфер обмена"/>
                <Button Content="Обновить" Margin="0,0,8,0"
                        Style="{StaticResource FlatButton}" Click="Refresh_Click"
                        ToolTip="Перечитать список модов и данные из Steam"/>
                <Button Content="Применить" Style="{StaticResource AccentButton}" Click="Apply_Click"/>
            </StackPanel>
        </Grid>
        </Grid>

        <Border x:Name="toast" Grid.RowSpan="2" Panel.ZIndex="50"
                HorizontalAlignment="Center" VerticalAlignment="Bottom"
                Margin="0,0,0,28" Visibility="Collapsed"
                Background="{StaticResource Panel}" CornerRadius="10"
                BorderBrush="{StaticResource Border}" BorderThickness="1">
            <Border.RenderTransform>
                <TranslateTransform x:Name="toastShift"/>
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="24" ShadowDepth="0" Opacity="0.55"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="toastBar" Grid.Column="0" Width="4" CornerRadius="2"
                        Background="{StaticResource Accent}" Margin="8,10,0,10"/>
                <TextBlock x:Name="toastIcon" Grid.Column="1" Text="✓" FontSize="15" FontWeight="Bold"
                           Foreground="{StaticResource Accent}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                <TextBlock x:Name="toastText" Grid.Column="2" VerticalAlignment="Center"
                           Foreground="{StaticResource Text}" FontSize="13" Margin="10,14,8,14"
                           MaxWidth="560" TextWrapping="Wrap"/>
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center"
                            Margin="4,0,8,0">
                    <Button x:Name="toastAction" Style="{StaticResource FlatButton}" Padding="11,5"
                            Margin="0,0,4,0" Visibility="Collapsed" Click="ToastAction_Click"/>
                    <Button x:Name="toastClose" Content="✕" Width="28" Height="28" Padding="0"
                            Style="{StaticResource FlatButton}" Background="Transparent"
                            Click="ToastClose_Click" ToolTip="Закрыть"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
    </Border>
</Window>
